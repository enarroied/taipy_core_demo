[project]
name = "taipy-core-demo"
version = "0.1.0"
description = "A Taipy app that shows the basics of Taipy Scenario Management pipelines"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "taipy>=4.1.0",
]

[dependency-groups]
dev = [
    "ruff>=0.14.0",
]
